#include <bits/stdc++.h>
using namespace std;

int main()
{
    vector<pair<int, string>> v; // {score, names}

    // Example usage (optional)
    // v.push_back({90, "Alice"});
    // v.push_back({85, "Bob"});

    sort(v.begin(), v.end());

    return 0;
}
